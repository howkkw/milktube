extends base
block contents
    form(method="POST", enctype="multipart/form-data").Form
        div.Form-column    
            span Video File
            input(type="file", required, name="video", id="video", accept="video/*")
        input(type="submit", value="Edit Video File").Form-btn